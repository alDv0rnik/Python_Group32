"""
Замените в списке [1, 2, 3, 4, 5] первые три элемента их суммой. Затем добавьте в конец списка число 7.
Переместите первый элемент конечного списка в предпоследнюю позицию списка. Выведите список на экран.
"""
# lst = [1, 2, 3, 4, 5]
# s = sum(lst[:3])
# lst[0] = s
# del lst[1:3]
# lst.append(7)
# lst.insert(2, lst.pop(0))
# print(lst)

lst = [0, 0, 0, 0, 0, 0]


for elem in lst:
    print(bool(elem))

print()
print(any(lst))